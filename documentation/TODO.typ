- [x] Add `dedup` example
- [x] Shadow check in `Context.insert`
- [x] Rewrite `Fold` to take a function
- [x] Test if call `add(val x = 42; x, x + y)` checks
- [x] Match multi
  - [ ] Remove `_` check in Context.merge et.al.
- [~] Pretty print `Expression`s
- [ ] Evaluation of `Fold` is right but must be left?
- [x] Unit and Void types
- [ ] Add tests for:
  - dup.id
  - dup.fun
  - dup.borrow
- [ ] Change `lookup` to `lookup!` on `Context`

== Delete

== Maybe

- [ ] Fold as function
  - Fix `Error.FoldMismatch`
