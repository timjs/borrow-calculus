// - Add `Compare` with `Nat -> Nat -> Bool` to `Expression`.
- Add `dedup` example.
- Shadow check in `Context.insert`.
- When splitting `Context` keep borrowed variables with `Protected` quantity.
- Fix `Context.merge` to remove `x_1` and `x_2` first.
- Should it only be possible to explicitly borrow linear variables? (See `Context.borrow`)
